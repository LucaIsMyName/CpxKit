{
  "name": "cpxkit",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "parcel serve index.html --dist-dir _serve/",
    "build": "parcel build index.html --dist-dir _build/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.12.0",
    "swiper": "^11.0.7"
  },
  "devDependencies": {
    "@parcel/transformer-inline-string": "^2.12.0",
    "@parcel/transformer-sass": "^2.12.0"
  }
}
